# 写作进度追踪表 · 设计文档

> 本文档是「写作进度追踪表」（`progress.py`）的设计依据，记录 2026-09-21 至 09-22 讨论中确定的全部规则。
> 实现前若有改动，先更新本文档；后续新增功能也在此追加。

---

## 一、目标与定位

**要解决的问题**

手上有若干本 LaTeX 笔记（多章多节、章节拆成独立 .tex 文件的结构），需要知道：每本写到哪了、哪一节还没动、自己在哪些地方留了欠账。这些问题靠翻目录看不出来。

**核心价值**

1. **揭示真实进度**——把「为加快编译而主动注释掉的章」与「从未动工的章」区分开。前者计入，后者排除，否则进度会被后者严重拉低。
2. **记住欠账**——默认认为已写完；只在你自己发现某处还没弄利索时标记，形成一份待补清单。

**交付形态**

本地 Python 服务（`127.0.0.1`）+ 浏览器页面。不部署公网，不做手机端。

---

## 二、技术架构

| 组件 | 作用 |
|---|---|
| `progress.py` | 服务本体。仅用 Python 标准库（`http.server` / `json` / `subprocess` / `pathlib`），零第三方依赖 |
| `progress_ui.html` | 前端页面。CSS/JS 全部内联、零外链，由服务在 `/` 路由下返回 |
| `progress.conf` | 配置：笔记根目录、端口、TeXStudio 路径、排除规则（首次运行自动生成） |
| `progress_marks.json` | 标记数据：三维标记、备注、待办、章统计覆盖、计划标记（运行时自动生成） |
| `progress.md` | 脚本使用说明（运行方式 / 实际输出 / 界面说明 / 行为细节） |

> 实现说明：前端拆为独立文件而非内嵌进 `progress.py`，便于单独编辑与语法高亮，降低单文件过大导致的出错风险。所有运行时数据文件（`progress.conf` / `progress_marks.json`）均由脚本自动生成，不入版本控制。

**启动方式**

- 主入口：若工作区已有总入口面板，可在其中增加一项调用本脚本。调起后**立即返回**，服务在后台运行。
- 次入口：命令行 `python progress.py`。
- **不做 `.bat`**。实测本机 `HKEY_CLASSES_ROOT\.py` 默认值未设置、`Python.File` 未注册打开命令，即 `.py` 无文件关联，双击不可靠。

**端口**

默认 `8765`，被占用则顺延（8766、8767…），可在 `progress.conf` 修改。启动后自动打开浏览器一次。

**重复启动**

检测到服务已在运行，则只打开浏览器，不再启动第二个进程。

**关闭**

页面提供「退出服务」按钮。服务停止时页面须显示明确提示（如"服务已停止"），不可静默失败。

**打开文件机制**

页面向本地服务发请求，服务用 `subprocess.Popen` 调起 `texstudio.exe "<路径>"`。

- 当 `.tex` 已在系统里关联 TeXStudio 时，注册表链为 `HKEY_CLASSES_ROOT\.tex` → ProgID → `"<TeXStudio 路径>" "%1"`，据此自动探测编辑器位置
- **不走 `file://` 链接**：Chrome 会把 `.tex` 当纯文本直接在页面里显示，不会交给外部程序。
- 移动端/窄屏隐藏该按钮（本设计不开放手机访问，此条为防御性规则）。

---

## 三、数据来源与扫描

### 3.1 解析方式

- 从每本笔记的 `main.tex` 出发，**递归展开 `\input` 树**，**不硬编码目录层级**。
  - 原因：各笔记目录深度不统一。有的为 `main → 章 index → 节 index → 小节`（3 层），有的为 `main → 节 index → 小节`（2 层，无章级 index）。
- 解析 `\input{...}` 必须做**花括号配对**，不得用 `[^}]*`。
  - 原因：路径可能含 LaTeX 花括号，如 `Content/7_The_additive_groups_R^{n}/index`。
- 解析出目标后，**不以 `Path.suffix` 判断是否要补 `.tex`**，改为「路径不以 `.tex` 结尾就补」。
  - 原因：文件名本身可能含点，如 `1_The_complexes_Ku,_K.u,_C,_Ku,_C`，后缀会算成 `.u,_C,_Ku,_C` 这种伪后缀，于是漏补 `.tex`，真实存在的文件被误判成「未建」。
- 用 `seen` 集合去重，防止同一目标被多个 `\input` 重复累加。
- 注释行（`%` 开头）中的 `\input` 记为「未纳入编译」，其条目信息保留，用于四象限判定。
  - **这一条对 `main.tex` 与各级 `index.tex` 一视同仁**。早期实现只在 `main.tex` 落地，各级 `index.tex` 中注释掉的 `\input` 被整棵跳过 —— 后果是它下面挂的节 / 小节在界面上**整块消失**（如某本笔记第 1 章注释掉的第 3、4 节，共 8 个文件从未出现在树里）。
- 既没被引用、也没以注释形式出现的 `.tex` 不进树（工作台反映的是笔记结构，不是目录内容），但收集起来在明细页底部单独列出。

### 3.2 判定「有内容」

剔除以下内容后，剩余字符数 **> 50** 即视为已写：

- 空行
- `%` 注释行
- `\part` / `\chapter` / `\section` / `\subsection` / `\subsubsection` 标题行
- `\input` 行

### 3.3 统计指标

- 定理环境数
- 环境内字符数 / 环境外字符数（陈述性文字）
- 行数
- 空环境数（环境内容 < 20 字符）
- `\begin` / `\end` 配对情况

### 3.4 定理环境清单

`structure.sty` 定义，共 17 个：

```
definition  axiom   hypothesis  theorem   lemma    proposition  corollary
metatheorem criteria problem    example   remark   exercise     algorithm
convention  alarm   sketch
```

- `proof` 与 `sketch` 特殊：用于追踪「证明」维度。
- 排版与数学环境（`enumerate`、`align*`、`itemize`、`tikzcd`、`cases` 等）视为内容的组成部分，**不计入环境数**。

### 3.5 排除规则

写入 `progress.conf` 可调：

- 隐藏目录、下划线开头的目录（如 `__pycache__`、`_backup_*`）
- 编译产物 23 种后缀（aux / log / out / toc / pdf 等）
- conf 中 `ignore` 列出的目录名

### 3.6 性能实测

实测（约 2200 个 tex 文件）：目录遍历约占 15%，逐文件读取统计占 85%，**合计亚秒级**。

结论：**每次打开页面或点刷新即全量重扫**，不做缓存与增量机制。

### 3.7 标题提取

| 层级 | 来源 | 覆盖率 |
|---|---|---|
| 章级 | `main.tex` 的 `\part{...}` | 100%（含被注释的章） |
| 节级 | 小节文件里的 `\chapter{...}` | 很低（视笔记习惯而定，有的接近 0） |
| 小节级 | `\section{...}` | 高（多数笔记接近 100%） |

- 缺失时以英文目录名 / 文件名兜底。
- 显示时须清理 `\texorpdfstring{$\R^{n}$}{Rn}` 这类包装（含数学符号的节标题里很常见）。

---

## 四、进度口径

### 4.1 小节三态

| 状态 | 判定 | 进度条颜色 |
|---|---|---|
| 已写 | 实质内容 > 50 字符 | 蓝 `#378ADD` |
| 已建空壳 | 文件存在但实质内容为 0 | 中灰 `#888780` |
| 规划未建 | 索引中有 `\input`，文件不存在 | 深灰 `#5F5E5A` |

实测全库落在 1–50 字符区间的文件仅 1 个、51–200 的仅 1 个，中间态几乎不存在，故阈值取 50 足够稳健。

### 4.2 章的四象限

| 未纳入编译 | 有已写内容 | 处理 |
|---|---|---|
| 否 | 有 | 计入 |
| 否 | 无 | 计入（占分母，待写） |
| **是** | **有** | **计入**，标注「未编译」（中性灰字，非警示色） |
| **是** | **无** | **排除**，不占分母 |

实测分布：被注释掉的顶层章里，多数完全无内容（排除），少数有内容（计入）；同一套规则也适用于中间层被注释的节。

同一套四象限**按层级递归**：早期只在顶层章上应用，各级 `index.tex` 中被注释的节 / 小节一律视为「计入」，与设计不符；现已统一。

### 4.3 手动覆盖（优先级：手动 > 自动）

每章一个统计状态：

| 状态 | 含义 |
|---|---|
| 自动（默认） | 按 4.2 规则判定 |
| 强制计入 | 无论自动判定如何，均计入统计 |
| 强制排除 | 无论自动判定如何，均排除 |

界面对手动设置过的章显示「手动」标记，与自动判定区分。

设置理由：自动统计会出错，且有时是系统性错误。实例——某本习题集的章节索引只引用了 2 个文件，于是算出「100%」，而实际有 247 个文件。此类问题自动规则永远修不好，必须留手动出口。反向的「强制计入」同样必要，以防自动误判为无内容。

### 4.4 百分比

- **结构进度** = 已写小节数 ÷ 计入章的规划小节数
- **确认完成率** = 已确认完成的小节数 ÷ 总小节数（受三维标记影响）

---

## 五、三维标记

### 5.1 三个维度（挂在小节级）

| 维度 | 含义 |
|---|---|
| 环境 | 该节的定理环境是否写完 |
| 正文 | 该节的陈述性文字（环境外的段落）是否写完 |
| 证明 | 该节的证明是否写完（`proof` / `sketch`） |

**默认值：全部为「已完成」。** 有内容的小节默认即完成，只在你自己发现没弄利索时手动改为「待补」。

### 5.2 形态

**不是复选框**（空框的默认态语义为"未完成"，与需求相反），而是可点击的**状态标记**：

- 已完成：低调的灰色对勾
- 待补：醒目的橙色空心圈 `#EF9F27`
- 点击即切换，再点切回

位置：列表行内并排三个；详情面板内以「标签 + 状态文字 + 标记」呈现。

### 5.3 批量开关

「证明」维度提供批量操作：把某章所有无 `proof` / `sketch` 的小节一次标为证明待补。

现状参考：实际写作中 `\begin{proof}` 与 `\begin{sketch}` 都很少见，多数小节的证明直接写成正文。

### 5.4 备注与待办（两者并存，分开两块）

| 类型 | 用途 | 形态 |
|---|---|---|
| 备注 | 说明性文字，如"这里暂时跳过，等写到第 5 节再回来补" | 多行自由文本 |
| 待办 | 动作条目，如"补第 3 个命题的证明" | 可勾选的列表，逐条添加 |

分开的理由：二者语义不同，混在一起会产生"一条没勾的备注算不算待办"这种说不清的状态。

- 备注**不参与进度计算**，纯备忘。
- 有小节的备注 / 待办时，列表行内显示一个小标记，详情面板内编辑。

### 5.5 章级标记

- 章行右侧显示**小节级标记的自动汇总**（如「3 节待补」，只读）。
- 章级另有一个**独立的手动标记**（如「整章待补」），用于不好归到某一节的问题。
- 两者并存，语义不同。章级标记**不是**"一键把整章所有小节标为待补"的批量开关（那会让汇总失真）。

### 5.6 计划标记

「设为下一步」——给某节打上标记，在续写区顶部突出显示。比备注更主动。

---

## 六、提示规则

### 6.1 两条硬提示（确定的问题）

1. `\begin` 与 `\end` 不配对（编译必然报错）
2. 环境内容为空（< 20 字符）

### 6.2 三条软提示（值得看一眼）

3. 环境数 < 同节其他小节中位数的 1/3
4. 同节其他小节都有 `proof` / `sketch`，本节没有
5. 同节其他小节都有陈述文字，本节为零

### 6.3 核心原则

**软提示只能用相对判断（同一「节」内各小节横向比较），绝不能用绝对阈值。**

原因：实测环境外文字整体只占 3.8%–5%，若按绝对阈值判断，几乎每一节都会亮灯，噪声淹没信号。若整个节都没有正文，那是用户的写法风格，不是异常。

### 6.4 呈现方式（三层）

| 层 | 形式 |
|---|---|
| 行内 | 小圆点：红＝硬问题，琥珀＝软提示；悬停显示原因 |
| 详情面板 | 「值得留意」区块，写明判断依据（如"环境 3 个，本节其他小节平均 9 个"） |
| 筛选 | 顶部筛选项增加「待补 / 可疑」 |

### 6.5 中性的信息展示

详情面板直接显示「环境 N 个 / 陈述文字 M 字符」，为 0 时用灰字标出——这是中性信息，不是警告。

---

## 七、界面设计

### 7.1 导航

**两项**：总览、明细。PC 用左侧边栏切换，窄屏用底部 Tab。

**续写区不设独立导航项**，而是占据总览页顶部。

### 7.2 顶部状态条（固定）

最后扫描时间 + 刷新按钮 + 服务状态 + 亮暗切换 + 导出报告。

### 7.3 总览页

自上而下：

1. **续写区**（置顶）
   - 最近改动的 N 个文件（按内容哈希判断真实改动，纯改名不算）
   - 停滞提醒：某本笔记超过 N 天无改动则标出
   - 「设为下一步」的节
2. **全库合计卡片**
   - 总小节数、已写数、待补数、定理环境总数
3. **9 张笔记卡片**
   - 字段：笔记名、三段式进度条 + 百分比、已写/规划小节数、定理环境数、总字数、未编译章数、待补标记数、停滞提醒
   - **不含「最后改动时间」字段**（该信息仅用于排序与停滞判断）
   - 未启用章数用灰字体现
   - 点击卡片 → 跳明细页并定位到最近改动的那一章
4. **排序**：默认按最近改动降序，可切换按名称

### 7.4 明细页

- **左：三层树**（章 → 节 → 小节），手风琴式
  - 默认只渲染章级；**默认展开最近改动的那一章**
  - **未启用的章默认也进树**（灰显 + 章名后标「未启用」），其下小节照常可展开、可点开、可标记，只是不计入统计 —— 否则这些小节在页面上彻底看不到，会被当成「工作台没显示全」。工具栏「未启用章」按钮可收起它们
  - 章行：显示名 + 三段式迷你条 + 已写/总数 + 小节级待补汇总 + 章级标记
  - 小节行：状态点 + 显示名 + 行尾三个标记
  - 未建条目**按序号插入原位**，不排到末尾（这样能看出"写到第 8 节断了"的连续断口）
  - 排序按数字前缀自然序（须处理 `010_` 这类带前导零的历史目录，不能按字符串排）
- **右：详情面板**（PC 固定栏，窄屏底部抽屉）
  - 标题、路径、三个标记、统计数字、备注、待办、操作按钮
- **顶部**：筛选（全部 / 未写 / 已写 / 未建 / 待补可疑）+ 搜索框 + 两个开关（「编辑范围」、「未启用章」）
- **底部**：两个默认折叠的分组
  - 「未启用章节（N）」：显示章名、小节数、状态说明、排除原因（如"域论 · 95 个文件 · 全为空"）
  - 「未纳入索引的文件（N）」：既没写进 `index.tex` 也没以注释形式出现的游离 `.tex`。不进树（不属于笔记结构），但必须列出，否则会被误认为「没显示全」

### 7.5 节点显示名与编号规则（2026-09-22 补充）

**显示名的来源**（本笔记体系有两种写法，都要覆盖）：

| 节点 | 显示名 |
|---|---|
| 顶层节点 | 内容本身是章时用 `\chapter{...}`；否则用 `main.tex` 的 `\part{...}` |
| 目录节点 | 自身或子树中第一个非空 `\chapter{...}` |
| 小节（叶子） | `\section{...}` |
| 兜底 | 去掉编号前缀的目录名 / 文件名（如 `1_Terms_and_relations` → `Terms and relations`） |

> **两种内容组织方式**：① `\chapter` 写在小节文件里（节内容拆分为多个文件，目录下另有 `index.tex` 汇总）；② 内容直接写在节目录自己的 `index.tex` 里（该目录下只有 `index.tex` 一个文件）。第 ② 种情况下 `index.tex` **没有 `\input` 子引用，但它仍然是容器**——`index.tex` 位于 `\input{.../xxx/index}` 的目标位置，这个形式本身就说明 `xxx` 是目录。若把「无子引用」等同于「叶子」，会连锁引发三个错误：该章失去章号而被编成小节（如误显示 `61.1`）、其父节点被误判为章级（导致同级真正的章全部失去编号）、自身的中文标题不被使用而回退成英文文件名。

> 层级判据 `node_level()`：自身带 `\chapter` → chapter；直接子节点里已有叶子带 `\chapter` → chapter；其余容器 → part。与树深度无关，故能同时适配两层与三层结构。

**编号按笔记的完整结构**：

- **部分号**用中文数字并写入显示名（`第一部分：数学的形式化描述`）。
- 章号在**全书内**递增（`1`、`2`…），节号为「章号.节内序号」（`1.1`、`1.2`…）。
- **被 `main.tex` 注释掉的 `\input` 同样参与编号**。这一点与「按 LaTeX 计数器编号」不同，是刻意的：编号要反映**笔记的章节结构**，而不是编译产物。若按计数器走，被注释掉的首章会拿不到编号，后面各章整体前移，与作者对笔记结构的认知相悖。是否纳入编译另由「未编译」标注体现。
- **只有实际存在的文件占节号** —— 未建的文件在 PDF 里不存在，不占号。
- **部分的判定**：顶层节点带 `\part` 名且该名只对应这一个 `\input` → 是部分；一个 `\part` 下挂多个 `\input` 时，它们各自是独立的章。不带宽 `\part` 名的再按内容层级判定。

### 7.6 未启用章节分组

- 位置：明细页底部，默认折叠
- 内容：章名、小节数、状态说明、排除原因
- 手动排除的与自动排除的用不同标记区分
- 这些章已在上方树里灰显列出，此分组只作集中说明与一键「计入」的入口

**「未纳入索引的文件」分组**（2026-09-22 补充）

- 同样位于明细页底部、默认折叠
- 收录：笔记目录下存在、但既没被任何 `\input` 引用、也没以 `%` 注释形式出现的 `.tex`
- 不进树 —— 工作台反映的是笔记的章节结构（由 main.tex 与各级 index.tex 定义），不是目录里有什么就显示什么
- 列出文件名、状态（有内容 / 空）、字数、最后修改，可一键用 TeXStudio 打开
- 来历：写到一半还没挂进索引的小节，或改名后忘删的旧文件

### 7.7 配色

整体为「蓝—灰」体系：

| 用途 | 颜色 |
|---|---|
| 已写 | 蓝 `#378ADD` |
| 已建空壳 | 中灰 `#888780` |
| 规划未建 | 深灰 `#5F5E5A` |
| 待补标记 | 橙 `#EF9F27`（全屏唯一暖色） |

- 小节状态点复用同一语义：蓝实心 / 灰实心 / 灰空心
- 主色本身用中性灰，让色彩只服务于状态
- 亮暗模式：跟随系统 + 手动切换按钮，记住选择

### 7.8 排版规格

- 行高紧凑：小节行 28–30px，章节行 34px
- 统计数字用等宽字体栈，保证一列数字对齐
- 字号：正文 13px、标题 14–15px、辅助信息 12px
- 基础窄屏响应式**保留**（窗口拖窄不破版），但**不做移动端专属交互**（逐层下钻、底部导航等已取消）

---

## 八、数据结构

### 8.1 `progress_marks.json`

```json
{
  "version": 1,
  "updated": "2026-09-22T10:26:47+08:00",
  "chapters": {
    "<笔记>/Content/8_Fields_Theory": {"mode": "force_exclude", "manual": true}
  },
  "entries": {
    "<笔记>/Content/3_.../2_preorder_relations.tex": {
      "marks": {"env": "done", "body": "done", "proof": "todo"},
      "note": "这里暂时跳过，等写到第 5 节再回来补",
      "todos": [
        {"text": "补第 3 个命题的证明", "done": false},
        {"text": "核对定义 2 的措辞", "done": true}
      ],
      "planned": true,
      "fingerprint": "<内容指纹，用于路径迁移>"
    }
  }
}
```

- `mode` 取值：`auto` / `force_include` / `force_exclude`
- `marks` 取值：`done` / `todo`
- `fingerprint`：由文件内容生成，用于目录改名后的标记迁移

### 8.2 写入健壮性

- **原子写入**：先写临时文件再替换，防止写一半中断导致 JSON 损坏
- 每次写入自动留一份 `.bak`
- 启动时校验 JSON，损坏则从 `.bak` 恢复并提示

### 8.3 备份

- 页面提供「导出 / 导入 JSON」按钮
- 导入时按**时间戳合并**（新数据覆盖旧数据），不丢失任一侧记录

---

## 九、笔记目录变动的处理

| 情形 | 处理 |
|---|---|
| 新增笔记 | 扫描时自动识别（含 `main.tex` 且有 `Content/`），无需手动注册 |
| 目录改名 | **迁移功能**：按**内容指纹**匹配，把旧路径的标记迁到新路径上。用户按项目惯例频繁加编号前缀，此功能必要 |
| 笔记删除 | 标记保留（可能只是临时移走），看板上不再出现该笔记 |
| 无法匹配的标记 | 保留记录并标为「已失效」，**不自动删除** |

---

## 十、已定决策汇总

### 10.1 工具与启动

| 项 | 决定 |
|---|---|
| 名称 | 界面标题「写作进度追踪表」；脚本 `progress.py` |
| repo | `note_progress_monitor` |
| 归位 | 工作区根目录，与其余笔记工具脚本并列 |
| 配套 | `progress.conf` + `progress.md`（脚本说明）+ README |
| 启动 | 不做 `.bat`；入口为命令行 |
| 端口 | 8765，占用顺延，conf 可改 |
| 开机自启 | 不做 |

### 10.2 界面

| 项 | 决定 |
|---|---|
| 导航 | 两项：总览、明细 |
| 续写区 | 置于总览页顶部，非独立导航项 |
| 详情面板 | PC 右侧固定栏、窄屏底部抽屉 |
| 配色 | 蓝—灰体系，橙为待补专用 |
| 进度条 | 三段式 |
| 行密度 | 紧凑 |
| 快捷键 | 一期只做 `/` 聚焦搜索、`Esc` 收起详情 |
| 移动端 | 不开放（仅监听 127.0.0.1），保留基础响应式 |

### 10.3 功能

| 项 | 决定 |
|---|---|
| 三维标记 | 环境 / 正文 / 证明，默认已完成 |
| 备注与待办 | 两者并存，分开两块 |
| 备注参与计算 | 否 |
| 计划标记 | 做（设为下一步） |
| 章级标记 | 做（独立标记 + 小节级自动汇总） |
| 章统计覆盖 | 自动 / 强制计入 / 强制排除 |
| 提示 | 2 硬 3 软，软提示须相对判断 |
| 导出报告 | Markdown（全库概况 + 各笔记待补清单 + 未启用章说明），页面按钮触发 |
| 数据存储 | 服务端 `progress_marks.json` |

---

## 十一、未纳入本期（后续可加）

| 功能 | 说明 |
|---|---|
| 手机端访问 | 需绑局域网 IP、放行防火墙、生成二维码；当前不做 |
| 移动端专属交互 | 逐层下钻、底部抽屉等；当前不做 |
| 自定义 URL 协议 | 注册表注册后可在任何页面调起 TeXStudio；当前不做 |
| 累计进度曲线 | 已写小节数随时间的折线；当前不做（砍掉了写作量热力图，因其统计的是产量而非进度，且数学笔记按行数计量会系统性失真） |
| 规范体检 | 前导零目录（`010_`）、游离文件、命名不规范等；可作为独立功能另做 |
| 多笔记横向对比 | 当前只在总览页并列展示，无对比分析 |

---

## 十二、实现进度

主体功能已完成并实测通过（2026-09-22）：

- [x] `progress.py`：扫描模块（递归解析 `\input` 树 + 字符统计 + 环境统计）
- [x] `progress.py`：HTTP 服务（页面、数据、标记、章覆盖、打开文件、报告、退出）
- [x] `progress.py`：标记读写（原子写入 + `.bak` + 内容指纹迁移）
- [x] `progress_ui.html`：总览页（续写区 + 全库合计卡 + 笔记卡片）
- [x] `progress_ui.html`：明细页（递归树 + 详情面板 + 未启用章分组）
- [x] `progress_ui.html`：筛选、搜索、快捷键、亮暗切换
- [x] 导出 Markdown 报告
- [x] `progress.conf` 及默认值
- [x] `progress.md` 脚本说明文档
- [x] 实测验证（见下表）
- [x] 发布到 GitHub（`note_progress_monitor`）

### 实测记录

| 项目 | 结果 |
|---|---|
| 扫描耗时 | 亚秒级（约 2200 个 tex 文件） |
| 进度口径 | 剔除 `\begin`/`\end` 标签本身后再计字符 |
| 计入规则 | 自动判定与手动覆盖均按预期生效 |
| 标记读写 | 端到端通过：写入 → 读回 → 落盘 → 清理 |
| 章统计覆盖 | 强制计入与恢复自动均按预期生效 |
| 打开文件 | 服务端 `subprocess.Popen` 调起 TeXStudio；工作区外的路径被拒绝 |
| 界面渲染 | 顶栏、续写区、合计卡、笔记卡、三层树均正常 |
| 代码自检 | 前端 JS 语法通过；无外部链接；无 emoji；DOM 引用全部存在 |
| 性能优化 | 初版 2.87 秒 → 合并重复读取 + 正则预检查 → 1.20 秒 |

### 实现中修掉的问题

| 问题 | 原因 | 处理 |
|---|---|---|
| 标题清理不净（`经典代数学}`、`R^nRn的离散子群`） | 用 `re.sub` 只删命令名，未处理配对括号；`\texorpdfstring{A}{B}` 的第二参数是**独立括号组**，须整体跳过 | 改为花括号配对提取参数 |
| `empty_envs` 恒为 `None` | `file_metrics` 里字段名为 `env_empty`，`aggregate` 读的是 `empty_envs` | 统一为 `empty_envs` |
| 详情面板备注/待办取不到 | `apply_marks` 未把 `note`/`todos`/`planned` 挂到叶子上 | 补上赋值 |
| 扫描慢（2.87 秒） | 同一文件被读两次（找 `\input` 与统计指标各一次）；每行无谓跑正则 | 复用已读文本；`\begin`/`\end` 先做字符串预检查 |
| 报 `SyntaxWarning: invalid escape sequence` | docstring 含 `\input`、`\cmd` 却非 raw 字符串 | 改为 `r"""..."""` |

### 与设计文档的偏差

| 项 | 设计 | 实现 |
|---|---|---|
| 前端存放 | 未明确（原计划内嵌） | 独立为 `progress_ui.html`，便于维护 |
